main:
  x <- 1
  y <- 2
  z <- 3

  r <- 1 #This declaration shouldn't be necessary; see what happens when you remove it
  c1 <~ bernoulli(1/3)
  c2 <~ bernoulli(1/3)
  if c1: 
    r <- x
  else:
    if c2:
      r <- y
    else:
      r <- z

  observe(~(r = 1))